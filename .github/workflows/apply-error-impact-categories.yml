name: Apply error impact categories

on:
  push:
    branches:
      - feature/error-impact-categories
    paths:
      - .github/workflows/apply-error-impact-categories.yml
      - scripts/apply-error-impact-categories.js
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: feature/error-impact-categories
          fetch-depth: 0

      - name: Apply patch
        run: node scripts/apply-error-impact-categories.js

      - name: Syntax check
        run: node --check admin-error-center.js

      - name: Verify Centro errori
        run: node scripts/check-error-center-runtime.js

      - name: Commit generated change
        run: |
          if git diff --quiet -- admin-error-center.js; then
            echo "No generated changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add admin-error-center.js
          git commit -m "feat: classify errors by operational impact"
          git push origin HEAD:feature/error-impact-categories
