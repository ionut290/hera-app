name: Audit commessa impianti listeners

on:
  push:
    branches: [opt/commesse-listener-audit-20260815]

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node scripts/audit-commessa-impianti-listeners.js
