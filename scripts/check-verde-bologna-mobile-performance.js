#!/usr/bin/env node
"use strict";

const assert = require("node:assert/strict");
const fs = require("node:fs");

const source = fs.readFileSync("verde-bologna-parchi-mobile.js", "utf8");
const baseSource = fs.readFileSync("verde-bologna.js", "utf8");
const operationalSource = fs.readFileSync("verde-bologna-operativo.js", "utf8");
const treeSource = fs.readFileSync("tree-search.js", "utf8");
const loader = fs.readFileSync("app-pure-utils.js", "utf8");
const html = fs.readFileSync("index.html", "utf8");
const sw = fs.readFileSync("sw.js", "utf8");

assert.match(source, /const SEARCH_DEBOUNCE_MS = 180;/);
assert.match(source, /const FETCH_TIMEOUT_MS = 8000;/);
assert.match(source, /const LIST_RENDER_LIMIT = 60;/);
assert.match(source, /const LABEL_MARKER_LIMIT = 80;/);
assert.match(source, /fieldValue\(record, \["quartiere", "nomequartiere", "nome_quartiere"\]\)/);
assert.doesNotMatch(source, /fetchAllRecords\(QUARTIERI_DATASET_ID\)/);
assert.match(source, /varga-verde-bologna:\$\{PARKS_DATASET_ID\}:0:plain:/);
assert.match(source, /const controller = typeof AbortController === "function"/);
assert.match(source, /Promise\.allSettled\(remainingOffsets\.map/);
assert.match(source, /publishParkRecords\(firstPage\.records\)/);
assert.doesNotMatch(source, /while \(offset < total/);
assert.match(source, /state\.filtered\.slice\(0, LIST_RENDER_LIMIT\)/);
assert.match(source, /window\.L\.canvas\(\{ padding: 0\.5 \}\)/);
assert.match(source, /state\.map\.on\("zoomend", scheduleViewportRefresh\)/);
assert.match(source, /renderMapMarkers\(\{ fitMap: false \}\)/);
assert.match(source, /!fitMap && state\.map\.getZoom\(\) >= 15/);
assert.match(source, /state\.filterTimer = window\.setTimeout\(applyLiveFilter, SEARCH_DEBOUNCE_MS\)/);
assert.match(source, /removeLegacyQuartieriCache\(\)/);
assert.match(source, /function geometryPriority\(geometry\)/);
assert.match(source, /if \(geometryPriority\(geometry\) > geometryPriority\(selected\)\) selected = geometry;/);
assert.match(source, /function isTechnicalGeometryField\(key\)/);
assert.match(source, /!isTechnicalGeometryField\(key\)/);
assert.match(source, /MOSTRA CONFINI/);
assert.match(source, /state\.boundaryLayer/);
assert.match(source, /const MANAGED_AREAS_DATASET_ID = "un_gest";/);
assert.match(source, /within_distance\(geo_point_2d, geom'POINT\(/);
assert.match(source, /function selectManagedBoundary\(parkRecord, candidates\)/);
assert.match(source, /record\.__vbBoundaryGeometry = geometry \|\| null;/);
assert.match(source, /Cerco nel catasto comunale…/);
assert.match(source, /varga-verde-bologna:park-boundary:/);
assert.doesNotMatch(source, /Non disponibili nel dataset/);

const officialDatasets = [
  "un_gest", "alberi-manutenzioni", "popolazione-arborea", "siepi",
  "attrezzature_ludiche_ginniche_sportive", "arredo", "sgambatura_cani",
  "carta-tecnica-comunale-toponimi-parchi-e-giardini", "aree-verdi_entrate_centroidi",
  "aree-ortive", "verde_privato_urbanizzato"
];
officialDatasets.forEach((datasetId) => assert.match(baseSource, new RegExp(`id: "${datasetId.replace(/[.*+?^${}()|[\]\\]/g, "\\$&")}"`)));
assert.match(baseSource, /title: "Catasto alberi"/);
assert.match(operationalSource, /🌲 Catasto alberi/);
assert.doesNotMatch(baseSource, /Alberi singoli/i);
assert.doesNotMatch(operationalSource, /Alberi singoli/i);
assert.match(baseSource, /function geometryPriority\(geometry\)/);
assert.match(baseSource, /const VIEWPORT_MAX_RECORDS = 500;/);
assert.match(baseSource, /const MOBILE_VIEWPORT_MAX_RECORDS = 140;/);
assert.match(baseSource, /const VIEWPORT_LIST_LIMIT = 60;/);
assert.match(baseSource, /const MOBILE_FULL_GEOMETRY_LIMIT = 40;/);
assert.match(baseSource, /const MOBILE_LABEL_MARKER_LIMIT = 80;/);
assert.match(baseSource, /within_distance\(\$\{geoField\}, geom'POINT\(/);
assert.match(baseSource, /state\.map\.on\("moveend zoomend", scheduleViewportLoad\)/);
assert.match(baseSource, /if \(zoom < 15\)/);
assert.match(baseSource, /const viewportLimit = mobileView\(\) \? MOBILE_VIEWPORT_MAX_RECORDS : VIEWPORT_MAX_RECORDS;/);
assert.match(baseSource, /state\.records\.slice\(0, VIEWPORT_LIST_LIMIT\)/);
assert.match(baseSource, /const lightweight = mobileView\(\) && state\.records\.length > MOBILE_FULL_GEOMETRY_LIMIT;/);
assert.match(baseSource, /classList\.toggle\("is-interactive", mobileView\(\) \|\| state\.fullscreen\)/);
assert.match(baseSource, /state\.map\.dragging\?\.enable\?\.\(\);/);
assert.match(baseSource, /state\.map\.touchZoom\?\.enable\?\.\(\);/);
assert.match(baseSource, /\.verde-bologna-map\.is-interactive\{touch-action:none!important\}/);
assert.doesNotMatch(baseSource, /state\.map\.dragging\?\.disable/);
assert.match(operationalSource, /\.verde-bologna-map\{width:100%!important;height:calc\(100dvh - 126px\)!important;min-height:420px!important;border-radius:0!important;background:#e9eef4!important;touch-action:pan-y!important\}/);
assert.match(operationalSource, /\.verde-bologna-browser-head\{display:none!important\}/);
assert.match(operationalSource, /\.verde-bologna-map-toolbar\{position:absolute!important/);
assert.match(operationalSource, /\.verde-bologna-page:not\(\.is-category-open\) \.verde-bologna-datasets\{display:grid!important/);
assert.match(operationalSource, /\.verde-bologna-page\.is-category-open \.verde-bologna-browser\{display:block!important\}/);
assert.doesNotMatch(operationalSource, /function primeDefaultDataset/);
assert.match(source, /\.verde-bologna-page\.vb-parks-advanced \.verde-bologna-parks-tools\{display:block\}/);
assert.match(baseSource, /function destroyCategoryMap\(\)/);
assert.doesNotMatch(baseSource, /oldMap\?\.off\?\.\(\); oldMap\?\.remove\?\.\(\)/);
assert.match(baseSource, /"hera:verde-bologna-map-destroyed"/);
assert.match(baseSource, /function renewMapContainer\(\)/);
assert.match(baseSource, /mapNode\.replaceWith\(freshMapNode\)/);
assert.match(baseSource, /try \{ oldMap\?\.remove\?\.\(\); \} catch/);
assert.match(baseSource, /function showCategoryHub\(\{ scroll = true \} = \{\}\)/);
assert.match(baseSource, /state\.categoryOpen = true/);
assert.match(baseSource, /function resetCategoryData\(\)/);
assert.match(baseSource, /state\.featureLayer\?\.clearLayers\?\.\(\)/);
assert.match(baseSource, /const activation = \+\+state\.activationSerial/);
assert.match(baseSource, /requestAnimationFrame\(\(\) => \{/);
assert.match(baseSource, /activation !== state\.activationSerial/);
assert.match(baseSource, /setView\(\[44\.4949, 11\.3426\], 15\)/);
assert.match(baseSource, /const records = \[\.\.\.\(first\.results \|\| \[\]\)\]\.slice\(0, PAGE_SIZE\)/);
assert.match(baseSource, /"hera:verde-bologna-category-closed"/);
assert.match(baseSource, /node\.querySelectorAll\("\[data-vb-open\]"\)\.forEach/);
assert.match(baseSource, /event\.stopPropagation\(\);\s+openDataset\(button\.dataset\.vbOpen\)/);
assert.match(baseSource, /"hera:verde-bologna-map-created"/);
assert.match(baseSource, /"hera:verde-bologna-category-opened"/);
assert.doesNotMatch(operationalSource, /function captureMapFactory/);
assert.doesNotMatch(source, /function captureMapFactory/);
assert.doesNotMatch(operationalSource, /window\.L\.map =/);
assert.doesNotMatch(source, /window\.L\.map =/);
assert.match(baseSource, /if \(state\.categoryOpen\) showCategoryHub\(\); else closePage\(\)/);
assert.match(source, /state\.abortControllers\.forEach\(\(controller\) => controller\.abort\(\)\)/);
assert.match(source, /generation !== state\.generation \|\| !parksActive\(\)/);
assert.match(source, /state\.active = false/);
assert.match(source, /if \(!state\.complete\) state\.loaded = false/);
assert.match(source, /if \(state\.active && page\.classList\.contains\("vb-parks-advanced"\)\) return/);
assert.match(treeSource, /function destroyTreeMap\(\)/);
assert.match(treeSource, /sessionStorage\.getItem\(VERDE_BOLOGNA_RETURN_KEY\) === "1"/);
assert.match(treeSource, /const MOBILE_VISIBLE_TREE_LIMIT = 80;/);
assert.match(treeSource, /const visibleLimit = mobileView\(\) \? MOBILE_VISIBLE_TREE_LIMIT : DESKTOP_VISIBLE_TREE_LIMIT;/);
assert.match(treeSource, /requestAnimationFrame\(\(\) => \{/);
assert.match(treeSource, /window\.HeraTreeSearch = Object\.freeze\(\{ open: openPage, close: closePage \}\)/);
assert.doesNotMatch(treeSource, /offset < first\.total_count/);
assert.match(baseSource, /window\.HeraTreeSearch\?\.open/);
assert.match(baseSource, /requestAnimationFrame\(\(\) => openTreeSearch\(\)\)/);
assert.match(baseSource, /const DELEGATED_MAP_CLEANUP_DELAY_MS = 700;/);
const delegatedOpening = baseSource.match(/if \(dataset\.delegate\) \{[\s\S]*?\n      return;\n    \}/);
assert.ok(delegatedOpening, "Apertura delegata Catasto alberi non trovata");
assert.match(delegatedOpening[0], /stopCategoryWork\(\)/);
assert.match(delegatedOpening[0], /page\?\.classList\.add\("hidden"\)/);
assert.match(delegatedOpening[0], /window\.setTimeout\(\(\) => \{/);
assert.ok(delegatedOpening[0].indexOf('page?.classList.add("hidden")') < delegatedOpening[0].indexOf("destroyCategoryMap()"), "La vista Catasto deve aprirsi prima della distruzione della vecchia mappa");
assert.match(treeSource, /const MOBILE_MAP_START_DELAY_MS = 250;/);
assert.match(treeSource, /mobileView\(\) \? MOBILE_MAP_START_DELAY_MS : 0/);
assert.match(baseSource, /data-vb-detail-index/);
assert.match(baseSource, /data-vb-toggle-details/);
assert.match(baseSource, /entryIndex >= 6/);
assert.match(baseSource, /NAVIGA VERSO L’ELEMENTO/);
assert.match(baseSource, /VISTA 360° E PERCORSO/);
assert.match(baseSource, /id="verde-bologna-map-style"/);
assert.match(baseSource, /<option value="classic">Classica<\/option>/);
assert.match(baseSource, /<option value="satellite">Satellite<\/option>/);
assert.match(baseSource, /<option value="hybrid">Ibrida<\/option>/);
assert.match(operationalSource, /#verde-bologna-map \.verde-bologna-marker-wrap/);
assert.match(operationalSource, /classList\.contains\("vb-parks-advanced"\)/);
assert.match(source, /\.vb-parks-advanced \.verde-bologna-marker-wrap\{display:none!important\}/);

const activation = source.match(/function activateParksMode\(\) \{[\s\S]*?\n  \}\n\n  function deactivateParksMode/);
assert.ok(activation, "Funzione activateParksMode non trovata");
assert.doesNotMatch(activation[0], /requestUserPosition\(\)/);

const deactivation = source.match(/function deactivateParksMode\(\) \{[\s\S]*?\n  \}\n\n  function resetCategoryState/);
assert.ok(deactivation, "Funzione deactivateParksMode non trovata");
assert.match(deactivation[0], /if \(!state\.active && !hasAdvancedClass\) return/);
assert.match(deactivation[0], /if \(hasAdvancedClass\) page\.classList\.remove\("vb-parks-advanced"\)/);

const pageOpening = baseSource.match(/function openPage\(\) \{[\s\S]*?\n  \}\n\n  function closePage/);
assert.ok(pageOpening, "Funzione openPage Verde Bologna non trovata");
assert.match(pageOpening[0], /showCategoryHub\(\{ scroll: false \}\)/);
assert.doesNotMatch(pageOpening[0], /initializeMap\(|openDataset\(/);

assert.match(loader, /verde-bologna\.js\?v=20260901-cobo-sfalcio1/);
assert.match(loader, /verde-bologna-operativo\.js\?v=20260901-catasto-open2/);
assert.match(loader, /verde-bologna-parchi-mobile\.js\?v=20260901-cobo-sfalcio1/);
assert.match(html, /app-pure-utils\.js\?v=20260902-verde-levato2/);
assert.match(html, /PWA_EMERGENCY_CACHE_RESET_VERSION = "20260902-verde-levato2"/);
assert.match(html, /serviceWorker\.register\("\.\/sw\.js\?v=20260902-verde-levato2"/);
assert.match(sw, /const CACHE_NAME = "varga-cantieri-shell-v221";/);
assert.match(sw, /app-pure-utils\.js\?v=20260902-verde-levato2/);
assert.match(sw, /verde-bologna-parchi-mobile\.js\?v=20260901-cobo-sfalcio1/);
assert.match(sw, /"\/verde-bologna\.js"/);
assert.match(sw, /"\/verde-bologna-operativo\.js"/);
assert.match(sw, /"\/verde-bologna-parchi-mobile\.js"/);
assert.match(sw, /"\/tree-search\.js"/);

assert.doesNotMatch(source, /firebase|firestore|onSnapshot|addDoc|setDoc|updateDoc/);
assert.doesNotMatch(baseSource, /firebase|firestore|onSnapshot|addDoc|setDoc|updateDoc/);

console.log("Verde Bologna mobile performance checks passed.");
